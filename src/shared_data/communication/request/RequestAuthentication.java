package shared_data.communication.request;

import shared_data.communication.Request;

public class RequestAuthentication extends Request {

    public RequestAuthentication(String ip, int port) {
        super(ip, port);
    }
}
